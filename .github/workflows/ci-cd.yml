name: CI/CD Pipeline for Resume Analyzer

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt --quiet
          python -m spacy download en_core_web_sm --quiet

      - name: Run tests
        run: |
          echo "Running tests..."
          # Add test commands here (e.g., pytest, unittest)

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy with ngrok
        run: |
          echo "Deploying the app with ngrok..."
          pip install pyngrok --quiet
          ngrok authtoken 1nl3Mn1X5UJPxiTVpqqtNr0whc2_7fe21pBzrZ7kkeyBCLGko
          streamlit run app.py --server.port 8504 &>/dev/null& &
          ngrok http 8504